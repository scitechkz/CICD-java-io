This project will run a simple java welcome app from a docker container. 
Create a CICD pipeline that will build the app and deploy to docker hub.
Pipeline will be triggered by a code commit to github
 
pipeline structure
github => deploy to docker hub
